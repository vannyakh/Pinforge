# Pinforge uses pnpm. Keep installs strict and Electron downloads from defaults
# (do not set ELECTRON_MIRROR here — it can break electron-builder binary paths).
engine-strict=true
auto-install-peers=true
